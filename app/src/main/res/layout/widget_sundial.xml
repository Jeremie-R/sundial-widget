<?xml version="1.0" encoding="utf-8"?>
<!--
  The whole face (background shape, arcs, sun) is a pre-rendered bitmap on @id/widget_dial.
  Time and date are TextClocks so they stay exact between sun updates; the updater picks
  the heavy or medium time view, and sets colours and sizes, per look.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/widget_dial"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:importantForAccessibility="no"
        android:scaleType="fitCenter"
        android:src="@drawable/widget_preview" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center_horizontal"
        android:orientation="vertical">

        <TextClock
            android:id="@+id/widget_time"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-black"
            android:format12Hour="h:mm"
            android:format24Hour="HH:mm"
            android:includeFontPadding="false"
            android:textColor="@color/widget_on_surface"
            android:textSize="@dimen/widget_time_text_size" />

        <TextClock
            android:id="@+id/widget_time_medium"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-medium"
            android:format12Hour="h:mm"
            android:format24Hour="HH:mm"
            android:includeFontPadding="false"
            android:textColor="@color/widget_on_surface"
            android:textSize="@dimen/widget_time_text_size"
            android:visibility="gone" />

        <TextClock
            android:id="@+id/widget_date"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-medium"
            android:format12Hour="EEE d"
            android:format24Hour="EEE d"
            android:includeFontPadding="false"
            android:textColor="@color/widget_on_surface_variant"
            android:textSize="@dimen/widget_date_text_size" />
    </LinearLayout>

</FrameLayout>
